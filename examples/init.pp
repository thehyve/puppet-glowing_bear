# An example of a manifest file which only creates the glowingbear system user
# and its home directory and checks if the required parameters are provided
# (either in hiera or as parameters of the class instantiation).
#
include ::glowing_bear

